// -*- MPC -*-
// $Id: orbsvcs_output.mpb 14 2007-02-01 15:49:12Z mitza $

project {
  // We tested with all orbsvcs libs going to $(TAO_ROOT)/orbsvcs/lib, but
  // decided that it was easier to deal with all libs in one place.
  libout = $(ACE_ROOT)/lib
}
