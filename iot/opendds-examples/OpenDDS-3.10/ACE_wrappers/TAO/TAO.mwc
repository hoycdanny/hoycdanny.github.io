// $Id: TAO.mwc 1861 2011-08-31 16:18:08Z mesnierp $

workspace {
  exclude {
    CIAO
    DAnCE
  }

  // The associate scope does not add directories to the workspace;
  // it only associates a name with one or more directories.
  associate(examples) {
    examples
  }
  associate(tests) {
    performance-tests
    interop-tests
    tests
  }
}

